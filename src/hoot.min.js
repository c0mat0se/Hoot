function Hoot(offset){let elems=document.querySelectorAll('.hoot');elems.forEach(function(item){item.addEventListener('mouseover',tooltipsCreate);item.addEventListener('mouseout',tooltipsDestroy)});function tooltipsCreate(){let ttContent=this.dataset.tooltip;let ttPos=this.dataset.tooltipPosition;let tooltip=document.createElement('div');tooltip.setAttribute('id','hoot-tg');tooltip.setAttribute('class','hoot-tooltip');tooltip.setAttribute('data-position',ttPos);let ttText=document.createTextNode(ttContent);tooltip.appendChild(ttText);document.body.appendChild(tooltip);let hootOffsetTop=this.getBoundingClientRect().top,hootOffsetLeft=this.getBoundingClientRect().left,hootHeight=this.offsetHeight,hootWidth=this.offsetWidth;let tooltipHeight=tooltip.offsetHeight,tooltipWidth=tooltip.offsetWidth;let y='',x='';let ttOffset=8;if(!ttPos){ttPos='top'}
if(ttPos=='bottom'){y=(hootOffsetTop+hootHeight)+ttOffset;x=(hootOffsetLeft+(hootWidth/2))-(tooltipWidth/2)}
if(ttPos=='top'){y=(hootOffsetTop-tooltipHeight)-ttOffset;x=(hootOffsetLeft+(hootWidth/2))-(tooltipWidth/2)}
if(ttPos=='left'){y=(hootOffsetTop+(hootHeight/2))-(tooltipHeight/2);x=(hootOffsetLeft-tooltipWidth)-ttOffset}
if(ttPos=='right'){y=(hootOffsetTop+(hootHeight/2))-(tooltipHeight/2);x=(hootOffsetLeft+hootWidth)+ttOffset}
tooltip.style.top=Math.round(y)+'px';tooltip.style.left=Math.round(x)+'px'}
function tooltipsDestroy(){let tooltipDissolve=document.querySelectorAll('#hoot-tg');for(var z=0;z<tooltipDissolve.length;z++){tooltipDissolve[z].parentNode.removeChild(tooltipDissolve[z])}}}
